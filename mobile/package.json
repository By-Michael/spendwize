{
  "name": "spendwise-app",
  "version": "1.0.0",
  "description": "SpendWise — Capacitor wrapper around the static frontend, backed by the SpendWise PHP REST API on InfinityFree",
  "private": true,
  "scripts": {
    "sync": "cap sync",
    "android": "cap open android",
    "ios": "cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "@capacitor/android": "^6.0.0"
  }
}
